package com.amazon;

public class test {

	public static void main(String[] args) {

		
		int[] input = {4,23,5,3,2};
		
		//choose pivot
		
		//int pivot
		
		//recursively call sort on left of pivot
		//recursively call sort on right of pivot
		
		// choose the pivot again outsize the unsorted arrray 
		
		//swap elemsts left and right of pivot if not in order
		
		String pWord = "word,";
        StringBuilder sb = new StringBuilder(pWord);
        
        sb.deleteCharAt(sb.indexOf(","));
        
        System.out.println(sb);
				
	}
}
